{
  "position": "top",
  "layer": "top",
  "margin-top": 0,
  "margin-bottom": 0,
  "modules-left": ["hyprland/workspaces"],
  "modules-center": ["hyprland/window"],
  "modules-right": ["group/status"], 

  // --- THE UNIFIED RIGHT GROUP ---
  "group/status": {
    "orientation": "horizontal",
    "modules": [
        "tray",             // Added System Tray
        "network", 
        "bluetooth",
        "power-profiles-daemon",
        "battery", 
        "backlight",        // Added Brightness
        "pulseaudio",       // Speaker Volume
        "clock", 
        "custom/swaync"     // Notifications
    ]
  },

  // --- MODULE CONFIGURATIONS ---
  "hyprland/workspaces": {
    "format": "{icon}",
    "on-click": "activate"
  },
  "hyprland/window": {
    "format": "{}",
    "max-length": 50
  },
  "clock": {
    "format": "<big>•📆•</big> {:%a %d %H:%M}",
    "tooltip-format": "<big>{:%Y %B}</big>\n<tt><small>{calendar}</small></tt>"
  },
  "battery": {
    "states": { "warning": 30, "critical": 15 },
    "format": "<big>•{icon}•</big> {capacity}%",
    "format-warning": "<big>•{icon}•</big> {capacity}%",
    "format-critical": "<big>•{icon}•</big> {capacity}%",
    "format-charging": "<big>••</big> {capacity}%",
    "format-plugged": "<big>••</big> {capacity}%",
    "format-full": " {capacity}%",
    "format-alt": "<big>•{icon}•</big> {time}",
    "format-icons": ["", "", "", "", ""],
    "tooltip-format": "{time}"
  },
  "bluetooth": {
    "format": "<big>••</big> {status}",
    "format-disabled": "", // Hides if disabled
    "format-connected": "<big>••</big> {num_connections}",
    "tooltip-format": "{controller_alias}\t{controller_address}",
    "on-click": "bluedevil-wizard",
    "on-click-middle": "kcmshell6 kcm_bluetooth"
  },
  "network": {
    "format-wifi": "<big>••</big> {essid}",
    "format-ethernet": "<big>•🖥️•</big> {ifname}",
    "format-linked": "<big>••</big> {ifname} (No IP)",
    "format-disconnected": "<big>••</big> Disconnected",
    "tooltip-format-wifi": "Signal Strength: {signalStrength}%",
    "on-click": "kcmshell6 kcm_networkmanagement"
  },
  "pulseaudio": {
    "format": "<big>•{icon}•</big> {volume}%",
    "format-muted": "<big>•🚫🔈•</big>",
    "format-icons": {
        "default": ["🔈", "🔉"]
    },
    "on-click": "kcmshell6 kcm_pulseaudio"
  },
  // New Backlight Module
  "backlight": {
    "tooltip": false,
    "format": "<big>•{icon}•</big> {percent}%",
    "format-disabled": "", // Hides if disabled
    "format-icons": ["🌕", "🌔", "🌓", "🌒", "🌑"]
  },
  // New Tray Module
  "tray": {
    "icon-size": 18,
    "spacing": 10
  },
  "power-profiles-daemon": {
    "swap-icon-label": true,
    "format": "<big>•{icon}•</big> {profile}",
    "on-scroll-up": "",
    "on-scroll-down": "",
    "format-icons": {
      "default": "🛩︎",
      "performance": "🚀",
      "balanced": "🛩︎",
      "power-saver": "🌿"
    }
  },
  "custom/swaync": {
    "tooltip": true,
    "format": "<big>•{icon}•</big>",
    "format-icons": {
      "notification": "🔔",
      "none": "🔕",
      "dnd-notification": "🔔",
      "dnd-none": "🔕"
    },
    "return-type": "json",
    "exec-if": "which swaync-client",
    "exec": "swaync-client -swb",
    "on-click": "swaync-client -t -sw",
    "on-click-middle": "swaync-client -C",
    "on-click-right": "swaync-client -d -sw",
    "escape": true
  }
}
